export interface ConvertedAmount {
  id: string;
  amount: number;
  currency: string;
  result: number;
}

export type convertedAmount = {
  id: string;
  amount: number;
  currency: string;
  result: number;
};
